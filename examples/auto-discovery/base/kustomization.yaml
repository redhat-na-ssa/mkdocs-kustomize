apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

commonLabels:
  app: example-app
  environment: base

namePrefix: example-

images:
- name: nginx
  newTag: "1.21"
